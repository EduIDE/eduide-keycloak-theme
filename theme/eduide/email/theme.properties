# EduIDE email theme.
#
# parent=base for the same reason as the login theme: base/email ships no
# theme.properties, and its html/template.ftl is a bare
# <html><body><#nested></body></html> wrapper. Overriding that one wrapper
# brands every transactional mail, because all copy lives in message keys.
parent=base
locales=en,de
